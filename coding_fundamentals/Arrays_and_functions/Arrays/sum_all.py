# - Create a variable named `numbers`
#   with the following content: `[3, 4, 5, 6, 7]`
# - Print the sum of the elements in `numbers`

numbers = [3, 4, 5, 6, 7]
sum = sum(numbers)
print("the sum of the elements in numbers is: ", sum)