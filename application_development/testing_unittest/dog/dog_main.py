from application_development.test.dog.dog import Dog

dog = Dog("Furkesz", 5)
print(dog)

# As we had not given the string function in class Dog, we initially did not get the resired print result.
# Once we added the function in class Dog we get the required output.